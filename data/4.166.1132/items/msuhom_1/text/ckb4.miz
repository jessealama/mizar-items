environ
vocabularies UNIALG_1,NAT_1,XBOOLE_0,FUNCT_1,RELAT_1,TARSKI,SUBSET_1,FINSEQ_1,FUNCOP_1,PBOOLE,CARD_3,MSUALG_1,XXREAL_0,STRUCT_0,FINSEQ_2,PARTFUN1,CQC_SIM1,CARD_1,UNIALG_2,MARGREL1,NUMBERS,MSUALG_3,MEMBER_1,MSUHOM_1;
notations TARSKI,XBOOLE_0,SUBSET_1,NUMBERS,NAT_1,RELAT_1,FUNCT_1,PBOOLE,RELSET_1,PARTFUN1,FUNCT_2,CARD_3,FUNCOP_1,FINSEQ_1,STRUCT_0,UNIALG_1,UNIALG_2,FINSEQ_2,ALG_1,MSUALG_3,MSUALG_1,XXREAL_0;
definitions ALG_1,MSUALG_3,TARSKI,FUNCOP_1,FINSEQ_2,UNIALG_2;
theorems TARSKI,FINSEQ_2,MSUALG_1,UNIALG_2,MSUALG_3,FUNCOP_1,FINSEQ_1,FUNCT_1,FUNCT_2,ALG_1,UNIALG_1,PBOOLE,MONOID_1,CARD_3,RELAT_1,XBOOLE_0,XBOOLE_1,PARTFUN1,FINSEQ_3,CKB1,CKB2,CKB3;
registrations XBOOLE_0,FUNCT_1,ORDINAL1,RELSET_1,FUNCT_2,FUNCOP_1,XREAL_0,PRE_CIRC,STRUCT_0,MSUALG_1,MSUALG_3,RELAT_1,PBOOLE,FINSEQ_1;
constructors XXREAL_0,CARD_3,FINSEQOP,ALG_1,MSUALG_3,XREAL_0,RELSET_1,PBOOLE,NAT_1;
requirements NUMERALS,SUBSET,BOOLE;
begin
reserve a for set;
theorem
Th4: (for I being set holds (for C being (Subset of I) holds (for M being (ManySortedSet of I) holds ( ( M | C ) # ) = ( ( M # ) | ( C * ) ))))
proof
let I being set;
let C being (Subset of I);
let M being (ManySortedSet of I);
( dom ( M # ) ) = ( I * ) by PARTFUN1:def 2;
then ( dom ( ( M # ) | ( C * ) ) ) = ( C * ) by CKB2:1,RELAT_1:62;
then reconsider D = ( ( M # ) | ( C * ) ) as (ManySortedSet of ( C * )) by PARTFUN1:def 2;
A1: ( C * ) c= ( I * ) by CKB2:1;
(for i being (Element of ( C * )) holds ( ( ( M # ) | ( C * ) ) . i ) = ( product ( ( M | C ) * i ) ))
proof
let i being (Element of ( C * ));
A2: ( rng i ) c= C;
i in ( C * );
then A3: i in ( dom D ) by PARTFUN1:def 2;
A4: i in ( I * ) by A1,TARSKI:def 3;
(a in ( D . i ) iff (ex g being Function st ((a = g & ( dom g ) = ( dom ( ( M | C ) * i ) )) & (for a holds (a in ( dom ( ( M | C ) * i ) ) implies ( g . a ) in ( ( ( M | C ) * i ) . a ))))))
proof
hereby
assume a in ( D . i );
then a in ( ( M # ) . i ) by A3,FUNCT_1:47;
then a in ( product ( M * i ) ) by A4,FINSEQ_2:def 5;
then consider g being Function such that A5: a = g and A6: ( dom g ) = ( dom ( M * i ) ) and A7: (for x being set holds (x in ( dom ( M * i ) ) implies ( g . x ) in ( ( M * i ) . x ))) by CARD_3:def 5;
take g;
thus a = g by A5;
( rng i ) c= C;
hence ( dom g ) = ( dom ( ( M | C ) * i ) ) by A6,CKB1:1;
thus (for a holds (a in ( dom ( ( M | C ) * i ) ) implies ( g . a ) in ( ( ( M | C ) * i ) . a )))
proof
A8: ( rng i ) c= C;
let a;
assume a in ( dom ( ( M | C ) * i ) );
then a in ( dom ( M * i ) ) by A8,CKB1:1;
then ( g . a ) in ( ( M * i ) . a ) by A7;
hence thesis by A8,CKB1:1;
end;

end;
given g being Function such that
A9: a = g
and
A10: ( dom g ) = ( dom ( ( M | C ) * i ) )
and
A11: (for a holds (a in ( dom ( ( M | C ) * i ) ) implies ( g . a ) in ( ( ( M | C ) * i ) . a )));

A12: (for a holds (a in ( dom ( M * i ) ) implies ( g . a ) in ( ( M * i ) . a )))
proof
let a;
assume a in ( dom ( M * i ) );
then a in ( dom ( ( M | C ) * i ) ) by A2,CKB1:1;
then ( g . a ) in ( ( ( M | C ) * i ) . a ) by A11;
hence thesis by A2,CKB1:1;
end;
( dom g ) = ( dom ( M * i ) ) by A2,A10,CKB1:1;
then a in ( product ( M * i ) ) by A9,A12,CARD_3:def 5;
then a in ( ( M # ) . i ) by A4,FINSEQ_2:def 5;
hence thesis by A3,FUNCT_1:47;
end;
hence thesis by CARD_3:def 5;
end;
hence ( ( M | C ) # ) = D by FINSEQ_2:def 5
.= ( ( M # ) | ( C * ) );
end;
