environ
vocabularies XBOOLE_0,ANALOAF,SUBSET_1,DIRAF,PARSP_2,FDIFF_1,SEMI_AF1;
notations STRUCT_0,ANALOAF,DIRAF,CKB1;
definitions CKB1;
theorems PARDEPAP,DIRAF,CKB1;
registrations ANALOAF,STRUCT_0;
constructors DIRAF,CKB1;
begin
registration
cluster  Semi_Affine_Space-like for non  empty non  empty AffinStruct;
existence
proof
consider SAS being AffinPlane such that A1: (((for o,a,a9,b,b9,c,c9 being (Element of SAS) holds ((((((((not o,a // o,b) & (not o,a // o,c)) & o,a // o,a9) & o,b // o,b9) & o,c // o,c9) & a,b // a9,b9) & a,c // a9,c9) implies b,c // b9,c9)) & (for a,a9,b,b9,c,c9 being (Element of SAS) holds (((((((not a,a9 // a,b) & (not a,a9 // a,c)) & a,a9 // b,b9) & a,a9 // c,c9) & a,b // a9,b9) & a,c // a9,c9) implies b,c // b9,c9))) & ((for a1,a2,a3,b1,b2,b3 being (Element of SAS) holds ((((a1,a2 // a1,a3 & b1,b2 // b1,b3) & a1,b2 // a2,b1) & a2,b3 // a3,b2) implies a3,b1 // a1,b3)) & (for a,b,c,d being (Element of SAS) holds ((((not a,b // a,c) & a,b // c,d) & a,c // b,d) implies (not a,d // b,c))))) by PARDEPAP:4;
reconsider AS = SAS as non  empty AffinStruct;
take AS;
thus (for a,b being (Element of AS) holds a,b // b,a) by DIRAF:40;
thus thesis by A1,DIRAF:40,PARDEPAP:5;
end;
end;
