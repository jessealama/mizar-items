environ
schemes CKB1,CKB2,CKB3,CKB4,CKB5;
begin
reserve a for set;
reserve b for set;
scheme Schemat18a { P[set],Q[set] } : (ex a st (for b holds (P[ a ] or Q[ b ])))
provided
A1: ((ex a st P[ a ]) or (for b holds Q[ b ]))
proof
now
A2:now
set a = the set;
assume (for b holds Q[ b ]);
then (for b holds (P[ a ] or Q[ b ]));
hence thesis;
end;
now
given a such that
A3: P[ a ];

(for b holds (P[ a ] or Q[ b ])) by A3;
hence thesis;
end;
hence thesis by A1,A2;
end;
hence thesis;
end;
