environ
vocabularies RLVECT_1,SUBSET_1,REAL_1,RELAT_1,ARYTM_3,SUPINF_2,CARD_1,XCMPLX_0,ANPROJ_1,ARYTM_1,XBOOLE_0,FUNCT_2,NUMBERS,FUNCT_1,FUNCSDOM,ZFMISC_1,INCSP_1,COLLSP,RELAT_2,PARSP_2,VECTSP_1,AFF_2,ANALOAF,ANPROJ_2;
notations XBOOLE_0,SUBSET_1,NUMBERS,FUNCT_1,FUNCT_2,DOMAIN_1,STRUCT_0,RLVECT_1,REAL_1,FUNCSDOM,COLLSP,ANPROJ_1;
definitions COLLSP,ANPROJ_1,BINOP_1,FUNCSDOM,RLVECT_1,STRUCT_0,ALGSTR_0;
theorems RLVECT_1,FUNCT_2,FUNCSDOM,COLLSP,ANPROJ_1,ENUMSET1,XCMPLX_0,XCMPLX_1,FUNCOP_1,STRUCT_0,CKB1,CKB2,CKB3,CKB4,CKB5,CKB6,CKB7,CKB8,CKB9,CKB10;
schemes FUNCT_2;
registrations XBOOLE_0,SUBSET_1,RELSET_1,NUMBERS,STRUCT_0,COLLSP,REAL_1,ANPROJ_1,XREAL_0,ORDINAL1;
constructors DOMAIN_1,REAL_1,FUNCSDOM,BINOP_2,ANPROJ_1,RELSET_1;
requirements NUMERALS,SUBSET,BOOLE,ARITHM;
begin
reserve V for RealLinearSpace;
reserve p for (Element of V);
reserve q for (Element of V);
reserve r for (Element of V);
reserve s for (Element of V);
reserve u for (Element of V);
reserve v for (Element of V);
reserve w for (Element of V);
reserve y for (Element of V);
reserve a for Real;
reserve b for Real;
reserve c for Real;
reserve d for Real;
reserve a1 for Real;
reserve b1 for Real;
reserve c1 for Real;
reserve d1 for Real;
reserve a2 for Real;
reserve b2 for Real;
reserve c2 for Real;
reserve d2 for Real;
reserve a3 for Real;
reserve b3 for Real;
theorem
Th4: (((for w holds (ex a st (ex b st (ex c st (ex d st w = ( ( ( ( a * p ) + ( b * q ) ) + ( c * r ) ) + ( d * s ) )))))) & (for a holds (for b holds (for c holds (for d holds (( ( ( ( a * p ) + ( b * q ) ) + ( c * r ) ) + ( d * s ) ) = ( 0. V ) implies (((a = ( 0 ) & b = ( 0 )) & c = ( 0 )) & d = ( 0 )))))))) implies (for u holds (for v holds (((not u is  zero) & (not v is  zero)) implies (ex y st (ex w st ((((u,v,w are_LinDep  & q,r,y are_LinDep ) & p,w,y are_LinDep ) & (not y is  zero)) & (not w is  zero))))))))
proof
assume that
A1: (for w holds (ex a st (ex b st (ex c st (ex d st w = ( ( ( ( a * p ) + ( b * q ) ) + ( c * r ) ) + ( d * s ) ))))))
and
A2: (for a holds (for b holds (for c holds (for d holds (( ( ( ( a * p ) + ( b * q ) ) + ( c * r ) ) + ( d * s ) ) = ( 0. V ) implies (((a = ( 0 ) & b = ( 0 )) & c = ( 0 )) & d = ( 0 )))))));
A3: (not p is  zero) by A2,CKB3:1;
let u;
let v;
assume that
A4: (not u is  zero)
and
A5: (not v is  zero);
consider a1,b1,c1,d1 such that A6: u = ( ( ( ( a1 * p ) + ( b1 * q ) ) + ( c1 * r ) ) + ( d1 * s ) ) by A1;
(not p,q,r are_LinDep ) by A2,CKB3:1;
then A7: (not  are_Prop q,r) by ANPROJ_1:11;
A8: (not q is  zero) by A2,CKB3:1;
consider a2,b2,c2,d2 such that A9: v = ( ( ( ( a2 * p ) + ( b2 * q ) ) + ( c2 * r ) ) + ( d2 * s ) ) by A1;
A10: ( ( a3 * u ) + ( b3 * v ) ) = ( ( ( ( ( ( a3 * a1 ) + ( b3 * a2 ) ) * p ) + ( ( ( a3 * b1 ) + ( b3 * b2 ) ) * q ) ) + ( ( ( a3 * c1 ) + ( b3 * c2 ) ) * r ) ) + ( ( ( a3 * d1 ) + ( b3 * d2 ) ) * s ) )
proof
( a3 * u ) = ( ( ( ( ( a3 * a1 ) * p ) + ( ( a3 * b1 ) * q ) ) + ( ( a3 * c1 ) * r ) ) + ( ( a3 * d1 ) * s ) ) by A6,CKB7:1;
hence ( ( a3 * u ) + ( b3 * v ) ) = ( ( ( ( ( ( a3 * a1 ) * p ) + ( ( a3 * b1 ) * q ) ) + ( ( a3 * c1 ) * r ) ) + ( ( a3 * d1 ) * s ) ) + ( ( ( ( ( b3 * a2 ) * p ) + ( ( b3 * b2 ) * q ) ) + ( ( b3 * c2 ) * r ) ) + ( ( b3 * d2 ) * s ) ) ) by A9,CKB7:1
.= ( ( ( ( ( ( a3 * a1 ) * p ) + ( ( b3 * a2 ) * p ) ) + ( ( ( a3 * b1 ) * q ) + ( ( b3 * b2 ) * q ) ) ) + ( ( ( a3 * c1 ) * r ) + ( ( b3 * c2 ) * r ) ) ) + ( ( ( a3 * d1 ) * s ) + ( ( b3 * d2 ) * s ) ) ) by CKB8:1
.= ( ( ( ( ( ( a3 * a1 ) + ( b3 * a2 ) ) * p ) + ( ( ( a3 * b1 ) * q ) + ( ( b3 * b2 ) * q ) ) ) + ( ( ( a3 * c1 ) * r ) + ( ( b3 * c2 ) * r ) ) ) + ( ( ( a3 * d1 ) * s ) + ( ( b3 * d2 ) * s ) ) ) by RLVECT_1:def 6
.= ( ( ( ( ( ( a3 * a1 ) + ( b3 * a2 ) ) * p ) + ( ( ( a3 * b1 ) + ( b3 * b2 ) ) * q ) ) + ( ( ( a3 * c1 ) * r ) + ( ( b3 * c2 ) * r ) ) ) + ( ( ( a3 * d1 ) * s ) + ( ( b3 * d2 ) * s ) ) ) by RLVECT_1:def 6
.= ( ( ( ( ( ( a3 * a1 ) + ( b3 * a2 ) ) * p ) + ( ( ( a3 * b1 ) + ( b3 * b2 ) ) * q ) ) + ( ( ( a3 * c1 ) + ( b3 * c2 ) ) * r ) ) + ( ( ( a3 * d1 ) * s ) + ( ( b3 * d2 ) * s ) ) ) by RLVECT_1:def 6
.= ( ( ( ( ( ( a3 * a1 ) + ( b3 * a2 ) ) * p ) + ( ( ( a3 * b1 ) + ( b3 * b2 ) ) * q ) ) + ( ( ( a3 * c1 ) + ( b3 * c2 ) ) * r ) ) + ( ( ( a3 * d1 ) + ( b3 * d2 ) ) * s ) ) by RLVECT_1:def 6;
end;
A11: (not r is  zero) by A2,CKB3:1;
A12:now
assume A13: (not  are_Prop u,v);
(ex w st (((not w is  zero) & u,v,w are_LinDep ) & (ex A,B,C being Real st w = ( ( ( A * p ) + ( B * q ) ) + ( C * r ) ))))
proof
A14:now
set a3 = ( - ( d2 * ( d1 " ) ) );
set b3 = 1;
set w = ( ( a3 * u ) + ( b3 * v ) );
assume that
A15: d1 <> ( 0 )
and
A16: d2 <> ( 0 );
set A = ( ( a3 * a1 ) + ( b3 * a2 ) );
set B = ( ( a3 * b1 ) + ( b3 * b2 ) );
set C = ( ( a3 * c1 ) + ( b3 * c2 ) );
A17: ((A <> ( 0 ) or B <> ( 0 )) or C <> ( 0 ))
proof
A18: ( d2 * ( d1 " ) ) <> ( 0 )
proof
assume (not thesis);
then ( d1 " ) = ( 0 ) by A16,XCMPLX_1:6;
hence contradiction by A15,XCMPLX_1:202;
end;
A19: ( ( d2 * ( d1 " ) ) * d1 ) = ( d2 * ( ( d1 " ) * d1 ) )
.= ( d2 * 1 ) by A15,XCMPLX_0:def 7
.= d2;
assume A20: (not thesis);
then A21: ( - ( - ( ( d2 * ( d1 " ) ) * c1 ) ) ) = c2;
(( - ( - ( ( d2 * ( d1 " ) ) * a1 ) ) ) = a2 & ( - ( - ( ( d2 * ( d1 " ) ) * b1 ) ) ) = b2) by A20;
then ( ( d2 * ( d1 " ) ) * u ) = v by A6,A9,A21,A19,CKB7:1;
hence contradiction by A13,A18,ANPROJ_1:1;
end;
( ( a3 * d1 ) + ( b3 * d2 ) ) = ( ( - ( d2 * ( ( d1 " ) * d1 ) ) ) + d2 )
.= ( ( - ( d2 * 1 ) ) + d2 ) by A15,XCMPLX_0:def 7
.= ( 0 );
then A22: w = ( ( ( ( ( ( a3 * a1 ) + ( b3 * a2 ) ) * p ) + ( ( ( a3 * b1 ) + ( b3 * b2 ) ) * q ) ) + ( ( ( a3 * c1 ) + ( b3 * c2 ) ) * r ) ) + ( ( 0 ) * s ) ) by A10
.= ( ( ( ( ( ( a3 * a1 ) + ( b3 * a2 ) ) * p ) + ( ( ( a3 * b1 ) + ( b3 * b2 ) ) * q ) ) + ( ( ( a3 * c1 ) + ( b3 * c2 ) ) * r ) ) + ( 0. V ) ) by RLVECT_1:10
.= ( ( ( ( ( a3 * a1 ) + ( b3 * a2 ) ) * p ) + ( ( ( a3 * b1 ) + ( b3 * b2 ) ) * q ) ) + ( ( ( a3 * c1 ) + ( b3 * c2 ) ) * r ) ) by RLVECT_1:4;
then A23: w = ( ( ( ( A * p ) + ( B * q ) ) + ( C * r ) ) + ( 0. V ) ) by RLVECT_1:4
.= ( ( ( ( A * p ) + ( B * q ) ) + ( C * r ) ) + ( ( 0 ) * s ) ) by RLVECT_1:10;
A24: (not w is  zero)
proof
assume (not thesis);
then w = ( 0. V ) by STRUCT_0:def 12;
hence contradiction by A2,A23,A17;
end;
u,v,w are_LinDep  by A4,A5,A13,ANPROJ_1:6;
hence thesis by A22,A24;
end;
A25:now
assume A26: d2 = ( 0 );
take w = v;
A27: u,v,w are_LinDep  by ANPROJ_1:11;
w = ( ( ( ( a2 * p ) + ( b2 * q ) ) + ( c2 * r ) ) + ( 0. V ) ) by A9,A26,RLVECT_1:10
.= ( ( ( a2 * p ) + ( b2 * q ) ) + ( c2 * r ) ) by RLVECT_1:4;
hence thesis by A5,A27;
end;
now
assume A28: d1 = ( 0 );
take w = u;
A29: u,v,w are_LinDep  by ANPROJ_1:11;
w = ( ( ( ( a1 * p ) + ( b1 * q ) ) + ( c1 * r ) ) + ( 0. V ) ) by A6,A28,RLVECT_1:10
.= ( ( ( a1 * p ) + ( b1 * q ) ) + ( c1 * r ) ) by RLVECT_1:4;
hence thesis by A4,A29;
end;
hence thesis by A25,A14;
end;
then consider w such that A30: (not w is  zero) and A31: u,v,w are_LinDep  and A32: (ex A,B,C being Real st w = ( ( ( A * p ) + ( B * q ) ) + ( C * r ) ));
consider A being Real,B being Real,C being Real such that A33: w = ( ( ( A * p ) + ( B * q ) ) + ( C * r ) ) by A32;
A34:now
set b = 1;
set a = ( - A );
set y = ( ( a * p ) + ( b * w ) );
A35: y = ( ( ( ( a + ( b * A ) ) * p ) + ( ( b * B ) * q ) ) + ( ( b * C ) * r ) ) by A33,CKB10:1
.= ( ( ( 0. V ) + ( ( 1 * B ) * q ) ) + ( ( 1 * C ) * r ) ) by RLVECT_1:10
.= ( ( B * q ) + ( C * r ) ) by RLVECT_1:4;
assume A36: (not  are_Prop p,w);
then A37: p,w,y are_LinDep  by A3,A30,ANPROJ_1:6;
A38: (B <> ( 0 ) or C <> ( 0 ))
proof
assume (not thesis);
then A39: w = ( ( ( A * p ) + ( 0. V ) ) + ( ( 0 ) * r ) ) by A33,RLVECT_1:10
.= ( ( ( A * p ) + ( 0. V ) ) + ( 0. V ) ) by RLVECT_1:10
.= ( ( A * p ) + ( 0. V ) ) by RLVECT_1:4
.= ( A * p ) by RLVECT_1:4;
A <> ( 0 )
proof
assume (not thesis);
then w = ( 0. V ) by A39,RLVECT_1:10;
hence contradiction by A30;
end;
hence contradiction by A36,A39,ANPROJ_1:1;
end;
A40: (not y is  zero)
proof
assume (not thesis);
then ( 0. V ) = ( ( B * q ) + ( C * r ) ) by A35,STRUCT_0:def 12
.= ( ( ( 0. V ) + ( B * q ) ) + ( C * r ) ) by RLVECT_1:4
.= ( ( ( ( 0 ) * p ) + ( B * q ) ) + ( C * r ) ) by RLVECT_1:10
.= ( ( ( ( ( 0 ) * p ) + ( B * q ) ) + ( C * r ) ) + ( 0. V ) ) by RLVECT_1:4
.= ( ( ( ( ( 0 ) * p ) + ( B * q ) ) + ( C * r ) ) + ( ( 0 ) * s ) ) by RLVECT_1:10;
hence contradiction by A2,A38;
end;
q,r,y are_LinDep  by A8,A11,A7,A35,ANPROJ_1:6;
hence thesis by A30,A31,A40,A37;
end;
now
assume  are_Prop p,w;
then (q,r,q are_LinDep  & p,w,q are_LinDep ) by ANPROJ_1:11;
hence thesis by A8,A30,A31;
end;
hence thesis by A34;
end;
now
assume  are_Prop u,v;
then A41: u,v,p are_LinDep  by ANPROJ_1:11;
(q,r,q are_LinDep  & p,p,q are_LinDep ) by ANPROJ_1:11;
hence thesis by A3,A8,A41;
end;
hence thesis by A12;
end;
