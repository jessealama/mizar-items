environ
vocabularies NUMBERS,NAT_1,VECTSP_1,SUBSET_1,MATRIX_1,FINSEQ_1,TREES_1,INCSP_1,MATRIX11,RELAT_1,FUNCT_1,XXREAL_0,CARD_1,ARYTM_3,FINSEQ_2,STRUCT_0,ALGSTR_0,MESFUNC1,ZFMISC_1,GROUP_1,SUPINF_2,FVSUM_1,CARD_3,RVSUM_1,MATRIX_6,ARYTM_1,QC_LANG1,MATRIX12;
notations MATRIX_1,XXREAL_0,MATRIX11,TARSKI,NUMBERS,NAT_1,FUNCT_1,FINSEQ_1,ZFMISC_1,BINOP_1,STRUCT_0,ALGSTR_0,GROUP_1,VECTSP_1,FVSUM_1,FINSEQ_2,MATRIX_3,MATRIX_6,CKB2,CKB7,CKB10,CKB12,CKB13,CKB14,CKB25,CKB26;
definitions FINSEQ_1,MATRIX_1,FVSUM_1,ALGSTR_0,CKB2,CKB7,CKB10,CKB25,CKB26;
theorems MATRIX_6,MATRIX11,FINSEQ_1,FINSEQ_2,FVSUM_1,MATRIX_1,FINSEQOP,ZFMISC_1,MATRIX_2,FUNCOP_1,RLVECT_1,GROUP_1,MATRIX_3,MATRIX_4,MATRIXR1,VECTSP_2,ORDINAL1,CARD_1,CKB1,CKB2,CKB3,CKB4,CKB5,CKB6,CKB7,CKB8,CKB9,CKB10,CKB11,CKB15,CKB16,CKB17,CKB18,CKB19,CKB20,CKB21,CKB22,CKB23,CKB24,CKB25,CKB26;
registrations RELSET_1,STRUCT_0,FVSUM_1,VECTSP_1,ORDINAL1,XXREAL_0,NAT_1,FINSEQ_2,XBOOLE_0,CARD_1;
constructors MATRIX_6,MATRIX11,CAT_2,ALGSTR_1,FVSUM_1,XXREAL_0,RELSET_1,CKB2,CKB7,CKB10,CKB25,CKB26;
requirements NUMERALS,REAL,BOOLE,SUBSET;
begin
reserve j for Nat;
reserve k for Nat;
reserve l for Nat;
reserve n for Nat;
reserve m for Nat;
reserve i for Nat;
reserve K for Field;
reserve a for (Element of K);
reserve M for (Matrix of n,m,K);
reserve M1 for (Matrix of n,m,K);
reserve M2 for (Matrix of n,m,K);
definition
let n;
let m;
let K;
let M being (Matrix of n,m,K);
let l being Nat;
let k being Nat;
let a being (Element of K);
assume that
A1: l in ( Seg ( width M ) )
and
A2: k in ( Seg ( width M ) )
and
A3: n > ( 0 )
and
A4: m > ( 0 );
func RcolXScalar (M,l,k,a) -> (Matrix of n,m,K) means 
:Def6: (( len it ) = ( len M ) & (for i holds (for j holds ((i in ( dom M ) & j in ( Seg ( width M ) )) implies ((j = l implies ( it * (i,j) ) = ( ( a * ( M * (i,k) ) ) + ( M * (i,l) ) )) & (j <> l implies ( it * (i,j) ) = ( M * (i,j) )))))));
existence
proof
A5: ( width M ) = m by A3,MATRIX_1:23;
then A6: ( len ( M @ ) ) = m by A4,MATRIX_2:10;
A7: ( len M ) = n by A3,MATRIX_1:23;
then ( width ( M @ ) ) = n by A4,A5,MATRIX_2:10;
then ( M @ ) is (Matrix of m,n,K) by A4,A6,MATRIX_1:20;
then consider M1 being (Matrix of m,n,K) such that A8: M1 = ( M @ );
A9: ( width ( RlineXScalar (M1,l,k,a) ) ) = n by A4,MATRIX_1:23;
then A10: ( len ( ( RlineXScalar (M1,l,k,a) ) @ ) ) = n by A3,MATRIX_2:10;
( len ( RlineXScalar (M1,l,k,a) ) ) = m by A4,MATRIX_1:23;
then ( width ( ( RlineXScalar (M1,l,k,a) ) @ ) ) = m by A3,A9,MATRIX_2:10;
then ( ( RlineXScalar (M1,l,k,a) ) @ ) is (Matrix of n,m,K) by A3,A10,MATRIX_1:20;
then consider M2 being (Matrix of n,m,K) such that A11: M2 = ( ( RlineXScalar (M1,l,k,a) ) @ );
take M2;
(for i holds (for j holds ((i in ( dom M ) & j in ( Seg ( width M ) )) implies ((j = l implies ( M2 * (i,j) ) = ( ( a * ( M * (i,k) ) ) + ( M * (i,l) ) )) & (j <> l implies ( M2 * (i,j) ) = ( M * (i,j) ))))))
proof
let i;
let j;
assume that
A12: i in ( dom M )
and
A13: j in ( Seg ( width M ) );
A14: [ i,j ] in ( Indices M ) by A12,A13,ZFMISC_1:87;
then A15: [ j,i ] in ( Indices M1 ) by A8,MATRIX_1:def 6;
then A16: i in ( Seg ( width M1 ) ) by ZFMISC_1:87;
A17: ( len M1 ) = ( width M ) by A8,MATRIX_1:def 6;
then A18: k in ( dom M1 ) by A2,FINSEQ_1:def 3;
( dom ( RlineXScalar (M1,l,k,a) ) ) = ( Seg ( len ( RlineXScalar (M1,l,k,a) ) ) ) by FINSEQ_1:def 3
.= ( Seg ( len M1 ) ) by A18,CKB10:def 1
.= ( dom M1 ) by FINSEQ_1:def 3;
then A19: [ j,i ] in ( Indices ( RlineXScalar (M1,l,k,a) ) ) by A15,CKB3:1;
A20: l in ( dom M1 ) by A1,A17,FINSEQ_1:def 3;
thus (j = l implies ( M2 * (i,j) ) = ( ( a * ( M * (i,k) ) ) + ( M * (i,l) ) ))
proof
A21: [ i,k ] in ( Indices M ) by A2,A12,ZFMISC_1:87;
A22: [ i,l ] in ( Indices M ) by A1,A12,ZFMISC_1:87;
assume A23: j = l;
( M2 * (i,j) ) = ( ( RlineXScalar (M1,l,k,a) ) * (j,i) ) by A11,A19,MATRIX_1:def 6
.= ( ( a * ( M1 * (k,i) ) ) + ( M1 * (l,i) ) ) by A20,A18,A16,A23,CKB10:def 1
.= ( ( a * ( M * (i,k) ) ) + ( M1 * (l,i) ) ) by A8,A21,MATRIX_1:def 6
.= ( ( a * ( M * (i,k) ) ) + ( M * (i,l) ) ) by A8,A22,MATRIX_1:def 6;
hence thesis;
end;

A24: j in ( dom M1 ) by A15,ZFMISC_1:87;
thus (j <> l implies ( M2 * (i,j) ) = ( M * (i,j) ))
proof
assume A25: j <> l;
( M2 * (i,j) ) = ( ( RlineXScalar (M1,l,k,a) ) * (j,i) ) by A11,A19,MATRIX_1:def 6
.= ( M1 * (j,i) ) by A18,A24,A16,A25,CKB10:def 1
.= ( M * (i,j) ) by A8,A14,MATRIX_1:def 6;
hence thesis;
end;

end;
hence thesis by A3,A7,MATRIX_1:23;
end;
uniqueness
proof
let M1 being (Matrix of n,m,K);
let M2 being (Matrix of n,m,K);
assume that
( len M1 ) = ( len M )
and
A26: (for i holds (for j holds ((i in ( dom M ) & j in ( Seg ( width M ) )) implies ((j = l implies ( M1 * (i,j) ) = ( ( a * ( M * (i,k) ) ) + ( M * (i,l) ) )) & (j <> l implies ( M1 * (i,j) ) = ( M * (i,j) ))))))
and
( len M2 ) = ( len M )
and
A27: (for i holds (for j holds ((i in ( dom M ) & j in ( Seg ( width M ) )) implies ((j = l implies ( M2 * (i,j) ) = ( ( a * ( M * (i,k) ) ) + ( M * (i,l) ) )) & (j <> l implies ( M2 * (i,j) ) = ( M * (i,j) ))))));
(for i holds (for j holds ([ i,j ] in ( Indices M1 ) implies ( M1 * (i,j) ) = ( M2 * (i,j) ))))
proof
A28: ( Indices M ) = ( Indices M1 ) by MATRIX_1:26;
let i;
let j;
assume [ i,j ] in ( Indices M1 );
then A29: (i in ( dom M ) & j in ( Seg ( width M ) )) by A28,ZFMISC_1:87;
then A30: (j <> l implies ( M1 * (i,j) ) = ( M * (i,j) )) by A26;
(j = l implies ( M1 * (i,j) ) = ( ( a * ( M * (i,k) ) ) + ( M * (i,l) ) )) by A26,A29;
hence thesis by A27,A29,A30;
end;
hence thesis by MATRIX_1:27;
end;
end;
