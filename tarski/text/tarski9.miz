environ

vocabularies TARSYM3;
notations TARSKI8;
constructors TARSKI8;

begin

reserve x for set;
reserve y for set;
reserve z for set;
reserve u for set;
reserve X for set;
reserve Y for set;
reserve Z for set;

definition
  let X,Y;
  pred
    X,Y are_equipotent
  means
    ex Z st
      (for x st x in X ex y st y in Y & [x,y] in Z) &
      (for y st y in Y ex x st x in X & [x,y] in Z) &
      for x,y,z,u st [x,y] in Z & [z,u] in Z holds x = z iff y = u;
end;
