environ

vocabularies TARSYM2;

begin

reserve x for set;
reserve X for set;
reserve Y for set;

definition
  let X;
  func
    union X -> set
  means
    x in it iff ex Y st x in Y & Y in X;
  correctness;
end;
